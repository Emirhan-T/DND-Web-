Add a playable hex-grid tactical map system for GM and players.

Remove the old map section and get rid of the old square grid design with other extensions such as coloring squares and adding players, moving them etc..

The map must appear inside the existing section in the game area after a game is launched.

The map should include the two things together: A hex map where you can place things and a background as image which can also be generated by API call.

Do not move it to a new page unless I explicitly approve it.